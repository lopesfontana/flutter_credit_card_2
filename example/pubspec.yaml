name: flutter_credit_card_2_example
description: "Demonstrates how to use the flutter_credit_card_2 plugin."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  flutter_credit_card_2:
    path: ../

  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/

